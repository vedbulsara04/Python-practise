import random

random_integer = random.randint(1, 10)
print(f"The Random Integer is : {random_integer}")

random_float = random.random()
print(f"The Random float within the range 0 to 1 is : {random_float}")
