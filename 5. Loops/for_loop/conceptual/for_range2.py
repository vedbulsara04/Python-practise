# The range() function returns a sequence of numbers, 
# starting from 0 by default, and increments by 1 (by default), 
# and ends at a specified number(n -1).

# Without Increment part (start, end)

for num in range(2, 6):
    print(num)
